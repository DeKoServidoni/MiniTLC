<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="CardIcon">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
    </style>

    <style name="CardIcon.Event" parent="CardIcon">
        <item name="android:src">@drawable/bubble</item>
    </style>

    <style name="CardIcon.Calendar" parent="CardIcon">
        <item name="android:src">@drawable/calendar</item>
    </style>

    <style name="CardIcon.Map" parent="CardIcon">
        <item name="android:src">@drawable/map</item>
    </style>

    <style name="CardIcon.Facebook" parent="CardIcon">
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:src">@drawable/facebook</item>
    </style>

</resources>
